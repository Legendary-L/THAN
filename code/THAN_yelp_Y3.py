with open('../data/yelp/yelp_data_brurb.txt', 'r') as source_file:
    with open('../data/yelp/X_brurb_3.txt', 'w') as target_file:

        start_line = 5233  # 起始行号
        end_line = 7846  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 13080  # 起始行号
        end_line = 15693  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 20927  # 起始行号
        end_line = 23540  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 28774  # 起始行号
        end_line = 31387  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 36621  # 起始行号
        end_line = 39234  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 44468  # 起始行号
        end_line = 47081  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 52315  # 起始行号
        end_line = 54928  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 60162  # 起始行号
        end_line = 62775  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 68009  # 起始行号
        end_line = 70622  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 75856  # 起始行号
        end_line = 78469  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)
        #
        #
        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 83703  # 起始行号
        end_line = 86316  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 91550  # 起始行号
        end_line = 94163  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 99397  # 起始行号
        end_line = 102010  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 107244  # 起始行号
        end_line = 109857  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 115091  # 起始行号
        end_line = 117704  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)
        #
        # #         15
        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 122938  # 起始行号
        end_line = 125551  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 130785  # 起始行号
        end_line = 133398  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 138632  # 起始行号
        end_line = 141245  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 146479  # 起始行号
        end_line = 149092  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 154326  # 起始行号
        end_line = 156939  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)
        #
        #
        # #20
        # #
        # #
        # source_file.seek(0)  # 将文件指针重新定位到文件开头
        # start_line = 162173  # 起始行号
        # end_line = 164786  # 结束行号
        # # 遍历每一行并复制
        # for i, line in enumerate(source_file):
        #     if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
        #         target_file.write(line)
        #
        #
        # source_file.seek(0)  # 将文件指针重新定位到文件开头
        # start_line = 170020  # 起始行号
        # end_line = 172633  # 结束行号
        # # 遍历每一行并复制
        # for i, line in enumerate(source_file):
        #     if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
        #         target_file.write(line)
        #
        #
        # source_file.seek(0)  # 将文件指针重新定位到文件开头
        # start_line = 177867  # 起始行号
        # end_line = 180480  # 结束行号
        # # 遍历每一行并复制
        # for i, line in enumerate(source_file):
        #     if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
        #         target_file.write(line)
        #
        #
        # source_file.seek(0)  # 将文件指针重新定位到文件开头
        # start_line = 185714  # 起始行号
        # end_line = 188327  # 结束行号
        # # 遍历每一行并复制
        # for i, line in enumerate(source_file):
        #     if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
        #         target_file.write(line)
        #
        #
        # source_file.seek(0)  # 将文件指针重新定位到文件开头
        # start_line = 193561  # 起始行号
        # end_line = 196174  # 结束行号
        # # 遍历每一行并复制
        # for i, line in enumerate(source_file):
        #     if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
        #         target_file.write(line)
        # #
        # #
        # source_file.seek(0)  # 将文件指针重新定位到文件开头
        # start_line = 201408  # 起始行号
        # end_line = 204021  # 结束行号
        # # 遍历每一行并复制
        # for i, line in enumerate(source_file):
        #     if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
        #         target_file.write(line)
        #
        #
        # source_file.seek(0)  # 将文件指针重新定位到文件开头
        # start_line = 209255  # 起始行号
        # end_line = 211868  # 结束行号
        # # 遍历每一行并复制
        # for i, line in enumerate(source_file):
        #     if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
        #         target_file.write(line)
        #
        #
        # source_file.seek(0)  # 将文件指针重新定位到文件开头
        # start_line = 217102  # 起始行号
        # end_line = 219715  # 结束行号
        # # 遍历每一行并复制
        # for i, line in enumerate(source_file):
        #     if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
        #         target_file.write(line)
        #
        #
        # source_file.seek(0)  # 将文件指针重新定位到文件开头
        # start_line = 224949  # 起始行号
        # end_line = 227562  # 结束行号
        # # 遍历每一行并复制
        # for i, line in enumerate(source_file):
        #     if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
        #         target_file.write(line)
        #
        #
        # source_file.seek(0)  # 将文件指针重新定位到文件开头
        # start_line = 232796  # 起始行号
        # end_line = 235409  # 结束行号
        # # 遍历每一行并复制
        # for i, line in enumerate(source_file):
        #     if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
        #         target_file.write(line)