with open('../data/dblp/oriData/dblp_data_apcpa.txt', 'r') as source_file:
    with open('../data/dblp/oriData/X_apcpa_1.txt', 'w') as target_file:

        start_line = 3  # 起始行号
        end_line = 14477  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)

        start_line = 43433  # 起始行号
        end_line = 57907  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)

        start_line = 86863  # 起始行号
        end_line = 101337  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        start_line = 130293  # 起始行号
        end_line = 144767  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        start_line = 173723  # 起始行号
        end_line = 188197  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)

        start_line = 217153  # 起始行号
        end_line = 231627  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)

        start_line = 260583  # 起始行号
        end_line = 275057  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)

        start_line = 304013  # 起始行号
        end_line = 318487  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        start_line = 347443  # 起始行号
        end_line = 361917  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)

        start_line = 390873  # 起始行号
        end_line = 405347  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)

        # 10

        start_line = 434303  # 起始行号
        end_line = 448777  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)

        start_line = 477733  # 起始行号
        end_line = 492207  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)

        start_line = 521163  # 起始行号
        end_line = 535637  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)

        start_line = 564593  # 起始行号
        end_line = 579067  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)

        start_line = 608023  # 起始行号
        end_line = 622497  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)

        start_line = 651453  # 起始行号
        end_line = 665927  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)

        start_line = 694883  # 起始行号
        end_line = 709357  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)

        start_line = 738313  # 起始行号
        end_line = 752787  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)

        start_line = 781743  # 起始行号
        end_line = 796217  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)

        start_line = 825173  # 起始行号
        end_line = 839647  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)