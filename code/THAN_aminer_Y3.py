with open('../data/aminer/oriData/THAN_aminer_random_data.txt', 'r') as source_file:
    with open('../data/aminer/oriData/THAN_aminer_Y3.txt', 'w') as target_file:

        start_line = 1154  # 起始行号
        end_line = 1683  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 19353  # 起始行号
        end_line = 19882  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 37552  # 起始行号
        end_line = 38081  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 55751  # 起始行号
        end_line = 56280  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 73950  # 起始行号
        end_line = 74479  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 92149  # 起始行号
        end_line = 92678  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line =  110348  # 起始行号
        end_line = 110877  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 128547  # 起始行号
        end_line = 129076  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 146746  # 起始行号
        end_line = 147275  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 164945  # 起始行号
        end_line = 165474  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 183144  # 起始行号
        end_line = 183673  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 201343  # 起始行号
        end_line = 201872  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 219542  # 起始行号
        end_line = 220071  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 237741  # 起始行号
        end_line = 238270  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 255940  # 起始行号
        end_line = 256469  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 274139  # 起始行号
        end_line = 274668  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 292338  # 起始行号
        end_line = 292867  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 310537  # 起始行号
        end_line = 311066  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 328736  # 起始行号
        end_line = 329265  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 346935  # 起始行号
        end_line = 347464  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)