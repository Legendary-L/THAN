with open('../data/yelp/yelp_data_brurb.txt', 'r') as source_file:
    with open('../data/yelp/X_brurb_2.txt', 'w') as target_file:

        start_line = 2618  # 起始行号
        end_line = 5231  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 10465  # 起始行号
        end_line = 13078  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 18312  # 起始行号
        end_line = 20925  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 26159  # 起始行号
        end_line = 28772  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 34006  # 起始行号
        end_line = 36619  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 41853  # 起始行号
        end_line = 44466  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 49700  # 起始行号
        end_line = 52313  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 57547  # 起始行号
        end_line = 60160  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 65394  # 起始行号
        end_line = 68007  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 73241  # 起始行号
        end_line = 75854  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 81088  # 起始行号
        end_line = 83701  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 88935  # 起始行号
        end_line = 91548  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 96782  # 起始行号
        end_line = 99395  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 104629  # 起始行号
        end_line = 107242  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 112476  # 起始行号
        end_line = 115089  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)
        #
        # # 15
        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 120323  # 起始行号
        end_line = 122936  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 128170  # 起始行号
        end_line = 130783  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 136017  # 起始行号
        end_line = 138630  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)
        #
        #
        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 143864  # 起始行号
        end_line = 146477  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 151711  # 起始行号
        end_line = 154324  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)
        #
        # #20
        # #
        # #
        # source_file.seek(0)  # 将文件指针重新定位到文件开头
        # start_line = 159558  # 起始行号
        # end_line = 162171  # 结束行号
        # # 遍历每一行并复制
        # for i, line in enumerate(source_file):
        #     if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
        #         target_file.write(line)
        #
        #
        # source_file.seek(0)  # 将文件指针重新定位到文件开头
        # start_line = 167405  # 起始行号
        # end_line = 170018  # 结束行号
        # # 遍历每一行并复制
        # for i, line in enumerate(source_file):
        #     if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
        #         target_file.write(line)
        #
        #
        # source_file.seek(0)  # 将文件指针重新定位到文件开头
        # start_line = 175252  # 起始行号
        # end_line = 177865  # 结束行号
        # # 遍历每一行并复制
        # for i, line in enumerate(source_file):
        #     if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
        #         target_file.write(line)
        #
        #
        # source_file.seek(0)  # 将文件指针重新定位到文件开头
        # start_line = 183099  # 起始行号
        # end_line = 185712  # 结束行号
        # # 遍历每一行并复制
        # for i, line in enumerate(source_file):
        #     if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
        #         target_file.write(line)
        #
        #
        # source_file.seek(0)  # 将文件指针重新定位到文件开头
        # start_line = 190946  # 起始行号
        # end_line = 193559  # 结束行号
        # # 遍历每一行并复制
        # for i, line in enumerate(source_file):
        #     if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
        #         target_file.write(line)
        # #
        # #
        # source_file.seek(0)  # 将文件指针重新定位到文件开头
        # start_line = 198793  # 起始行号
        # end_line = 201406  # 结束行号
        # # 遍历每一行并复制
        # for i, line in enumerate(source_file):
        #     if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
        #         target_file.write(line)
        #
        #
        # source_file.seek(0)  # 将文件指针重新定位到文件开头
        # start_line = 206640  # 起始行号
        # end_line = 209253  # 结束行号
        # # 遍历每一行并复制
        # for i, line in enumerate(source_file):
        #     if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
        #         target_file.write(line)
        #
        #
        # source_file.seek(0)  # 将文件指针重新定位到文件开头
        # start_line = 214487  # 起始行号
        # end_line = 217100  # 结束行号
        # # 遍历每一行并复制
        # for i, line in enumerate(source_file):
        #     if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
        #         target_file.write(line)
        #
        # source_file.seek(0)  # 将文件指针重新定位到文件开头
        # start_line = 222334  # 起始行号
        # end_line = 224947  # 结束行号
        # # 遍历每一行并复制
        # for i, line in enumerate(source_file):
        #     if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
        #         target_file.write(line)
        #
        #
        # source_file.seek(0)  # 将文件指针重新定位到文件开头
        # start_line = 230181  # 起始行号
        # end_line = 232794  # 结束行号
        # # 遍历每一行并复制
        # for i, line in enumerate(source_file):
        #     if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
        #         target_file.write(line)