with open('../data/aminer/oriData/THAN_aminer_random_data.txt', 'r') as source_file:
    with open('../data/aminer/oriData/THAN_aminer_Y2.txt', 'w') as target_file:

        start_line = 632  # 起始行号
        end_line = 1152  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 18831  # 起始行号
        end_line = 19351  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 37030  # 起始行号
        end_line = 37550  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 55229  # 起始行号
        end_line = 55749  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 73428  # 起始行号
        end_line = 73948  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 91627  # 起始行号
        end_line = 92147  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line =  109826  # 起始行号
        end_line = 110346  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 128025  # 起始行号
        end_line = 128545  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 146224  # 起始行号
        end_line = 146744  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 164423  # 起始行号
        end_line = 164943  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 182622  # 起始行号
        end_line = 183142  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 200821  # 起始行号
        end_line = 201341  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 219020  # 起始行号
        end_line = 219540  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 237219  # 起始行号
        end_line = 237739  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 255418  # 起始行号
        end_line = 255938  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 273617  # 起始行号
        end_line = 274137  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 291816  # 起始行号
        end_line = 292336  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 310015  # 起始行号
        end_line = 310535  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 328214  # 起始行号
        end_line = 328734  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 346413  # 起始行号
        end_line = 346933  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)