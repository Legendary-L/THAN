with open('../data/yelp/yelp_data_brurb.txt', 'r') as source_file:
    with open('../data/yelp/X_brurb_1.txt', 'w') as target_file:

        start_line = 3  # 起始行号
        end_line = 2616  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 7850  # 起始行号
        end_line = 10463  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 15697  # 起始行号
        end_line = 18310  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 23544  # 起始行号
        end_line = 26157  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 31391  # 起始行号
        end_line = 34004  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 39238  # 起始行号
        end_line = 41851  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 47085  # 起始行号
        end_line = 49698  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 54932  # 起始行号
        end_line = 57545  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 62779  # 起始行号
        end_line = 65392  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 70626  # 起始行号
        end_line = 73239  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)
        #
        #
        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 78473  # 起始行号
        end_line = 81086  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 86320  # 起始行号
        end_line = 88933  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 94167  # 起始行号
        end_line = 96780  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 102014  # 起始行号
        end_line = 104627  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 109861  # 起始行号
        end_line = 112474  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)
        #
        #
        #
        # # 15
        # #
        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 117708  # 起始行号
        end_line = 120321  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 125555  # 起始行号
        end_line = 128168  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 133402  # 起始行号
        end_line = 136015  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 141249  # 起始行号
        end_line = 143862  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)


        source_file.seek(0)  # 将文件指针重新定位到文件开头
        start_line = 149096  # 起始行号
        end_line = 151709  # 结束行号
        # 遍历每一行并复制
        for i, line in enumerate(source_file):
            if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
                target_file.write(line)
        #
        # # 20
        #
        #
        # source_file.seek(0)  # 将文件指针重新定位到文件开头
        # start_line = 156943  # 起始行号
        # end_line = 159556  # 结束行号
        # # 遍历每一行并复制
        # for i, line in enumerate(source_file):
        #     if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
        #         target_file.write(line)
        #
        #
        # source_file.seek(0)  # 将文件指针重新定位到文件开头
        # start_line = 164790  # 起始行号
        # end_line = 167403  # 结束行号
        # # 遍历每一行并复制
        # for i, line in enumerate(source_file):
        #     if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
        #         target_file.write(line)
        #
        #
        # source_file.seek(0)  # 将文件指针重新定位到文件开头
        # start_line = 172637  # 起始行号
        # end_line = 175250  # 结束行号
        # # 遍历每一行并复制
        # for i, line in enumerate(source_file):
        #     if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
        #         target_file.write(line)
        #
        #
        # source_file.seek(0)  # 将文件指针重新定位到文件开头
        # start_line = 180484  # 起始行号
        # end_line = 183097  # 结束行号
        # # 遍历每一行并复制
        # for i, line in enumerate(source_file):
        #     if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
        #         target_file.write(line)
        #
        #
        # source_file.seek(0)  # 将文件指针重新定位到文件开头
        # start_line = 188331  # 起始行号
        # end_line = 190944  # 结束行号
        # # 遍历每一行并复制
        # for i, line in enumerate(source_file):
        #     if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
        #         target_file.write(line)
        # #
        # #
        # source_file.seek(0)  # 将文件指针重新定位到文件开头
        # start_line = 196178  # 起始行号
        # end_line = 198791  # 结束行号
        # # 遍历每一行并复制
        # for i, line in enumerate(source_file):
        #     if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
        #         target_file.write(line)
        #
        #
        # source_file.seek(0)  # 将文件指针重新定位到文件开头
        # start_line = 204025  # 起始行号
        # end_line = 206638  # 结束行号
        # # 遍历每一行并复制
        # for i, line in enumerate(source_file):
        #     if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
        #         target_file.write(line)
        #
        #
        # source_file.seek(0)  # 将文件指针重新定位到文件开头
        # start_line = 211872  # 起始行号
        # end_line = 214485  # 结束行号
        # # 遍历每一行并复制
        # for i, line in enumerate(source_file):
        #     if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
        #         target_file.write(line)
        #
        #
        # source_file.seek(0)  # 将文件指针重新定位到文件开头
        # start_line = 219719  # 起始行号
        # end_line = 222332  # 结束行号
        # # 遍历每一行并复制
        # for i, line in enumerate(source_file):
        #     if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
        #         target_file.write(line)
        #
        #
        # source_file.seek(0)  # 将文件指针重新定位到文件开头
        # start_line = 227566  # 起始行号
        # end_line = 230179  # 结束行号
        # # 遍历每一行并复制
        # for i, line in enumerate(source_file):
        #     if i >= start_line - 1 and i <= end_line - 1:  # 第i行在[start_line, end_line]范围内
        #         target_file.write(line)